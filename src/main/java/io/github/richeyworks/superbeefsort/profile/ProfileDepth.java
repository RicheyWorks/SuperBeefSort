package io.github.richeyworks.superbeefsort.profile;

/** How much work the profiler does. {@code SHALLOW} samples; {@code DEEP} scans every element. */
public enum ProfileDepth {
    SHALLOW,
    DEEP
}
